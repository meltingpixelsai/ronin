import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "RONIN - Solana Ecosystem Intelligence",
  description:
    "Autonomous AI agent that detects emerging narratives across the Solana ecosystem by cross-referencing on-chain data, developer activity, and market signals.",
  openGraph: {
    title: "RONIN - Solana Ecosystem Intelligence",
    description:
      "AI-powered narrative detection for the Solana ecosystem. Built autonomously by an AI agent.",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className="dark">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased bg-background text-foreground`}
      >
        {children}
      </body>
    </html>
  );
}
